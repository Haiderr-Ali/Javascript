const accountId = 1443 //const keyword means it will never change in js//
let accountEmail = "haider@gmail.com" //let and var both are used to store variable but var is old method so we always use let because it has larger scope//
var accountPassword = "12345"
accountCity = "jaipur"// in js we can also declare variable without 

console.log(accountId); // this is a method to print the output in js

// but what if we want many output to print at the same time? then we use,//
console.table([accountEmail,accountPassword,accountCity])